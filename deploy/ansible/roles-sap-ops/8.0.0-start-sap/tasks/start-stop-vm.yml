
# Start Azure VM.
# - block:
#   # Start the Azure VM
#   - name:                              "Start the VM"
#     azure_rm_virtualmachine:
#      resource_group:                   "{{ sap_system_configuration_name }}"
#      name:                             "{{ansible_hostname }}"
#      restarted:                        true    
    
#   when:
#   -  sap_ops == "start"
# Debug I am here
  - name:                                "Debug I am here"
    ansible.builtin.debug:
      msg:                                "I am here"
    when:                                 sap_ops == "START"

  - name:                            "Start the VM Based on the VM Agent Status"
    ansible.builtin.include_tasks:    roles-sap-ops/8.0.0-start-sap/tasks/vmrestart.yaml
    vars:
      subscriptionId:                "{{ ansible_env.AZURE_SUBSCRIPTION_ID }}"
      resourceGroup_name:            "{{ rgname }}"
      # vmName:                        "{{ item.name }}"
    # loop:           "{{ groups['all'] }}" 
    when:                sap_ops == "START"
